# 사고 기록

내가 이 저장소에서 낸 사고와 그 원인을 적어 둔다. **작업을 시작하기 전에 읽는다.**

감상을 적는 곳이 아니다. 같은 실수를 다시 하지 않으려면 무엇을 확인해야 하는지
적는 곳이다. 그래서 사고마다 **무슨 일이 있었나 → 왜 그랬나 → 무엇을 했어야 했나**
순으로 쓰고, 반복된 것은 아래 '되풀이한 패턴' 에 횟수로 모아 둔다.

새 사고가 생기면 여기에 덧붙인다. 지우지 않는다.

---

## 되풀이한 패턴

같은 뿌리에서 여러 번 났다. 이것부터 본다. 숫자는 아래 본문에 번호로 적힌
건수와 같다 — 세어 볼 수 있게 이름을 붙여 뒀다.

| # | 패턴 | 횟수 | 해당 사고 |
|---|---|---|---|
| P1 | 앞 명령이 실패했는데 뒤 명령이 그대로 실행됐다 | **4** | 1, 2, 3, 21 |
| P2 | 눌러 보지 않고 "검증했다" 고 보고했다 | **5** | 4, 5, 6, 7, 8 |
| P3 | 되돌릴 수 없는 것을 먼저 백업하지 않았다 | **2** | 9, 10 |
| P4 | 확인하지 않고 단정하거나 값을 채웠다 | **9** | 11 ~ 18, 22 |
| P5 | 확인 없이 덮어썼다 | **2** | 19, 20 |

**사고 22건.**

## P1 — 실패한 명령 뒤가 계속 실행됐다

### 1. `.env` 3개를 파괴했다 (가장 큰 사고)

**무슨 일** `git worktree add` 가 실패했고, 이어지는 `cd` 도 실패했다. 그런데 같은
줄에 이어 붙인 `ln -sfn A A` 는 그대로 실행되어 **자기 자신을 가리키는 심볼릭 링크**
가 만들어졌다. `.env`, `.env.dev`, `.env.ops` 세 파일의 내용이 사라졌다.

**왜** 명령을 `;` 와 줄바꿈으로 이었다. 앞이 실패해도 뒤가 실행된다.
그리고 되돌릴 수 없는 파일을 건드리면서 사본을 만들어 두지 않았다.

**복구** 운영 API 프로세스가 살아 있어서 `ps eww` 로 환경변수를 읽어 `.env` 를
되살렸다. `.env.dev` 는 되살리지 못해 새로 만들었고 `JWT_SECRET` 이 바뀌어
**개발 서버의 모든 로그인이 무효화됐다.** `.env.ops` 도 새로 만들었다.

**했어야 할 것**
- 명령은 `&&` 로 잇는다. 스크립트는 `set -euo pipefail` 로 연다.
- 되돌릴 수 없는 파일(`.env`, 키, 인증서)은 **건드리기 전에** 사본을 뜬다.
- `ln -sfn` 처럼 조용히 덮어쓰는 명령은 대상 경로를 먼저 출력해 확인한다.

### 2. `develop` 에 `git reset --hard` 를 쳤다

**무슨 일** `git checkout main` 이 실패했는데(운영 워크트리가 이미 체크아웃 중)
이어서 `git reset --hard v2/main` 이 `develop` 위에서 실행됐다.

**왜** P1 과 같다. 그리고 `reset --hard` 를 브랜치 확인 없이 썼다.

**복구** 전부 푸시돼 있어서 `git reset --hard v2/develop` 로 되돌렸다. 운이 좋았다.

**했어야 할 것** `reset --hard` 앞에는 항상 `git branch --show-current` 의 **출력을
직접 보고** 나서 친다. 체크아웃 실패는 곧바로 멈출 이유다.

### 3. 백엔드 커밋이 `develop` 에 직접 들어갔다

**무슨 일** `git checkout ehyun` 이 `package.json` 로컬 변경 때문에 실패했는데,
이어지는 `git add`·`git commit` 이 `develop` 위에서 실행됐다. 브랜치 규칙
(`백엔드는 ehyun`)을 어겼다.

**왜** P1. 게다가 `git push v2 ehyun` 은 **로컬 `ehyun` 참조를 밀기 때문에** 아무
일도 안 하고 성공한 것처럼 보였다. 성공 메시지를 믿고 넘어갔다.

**복구** 해당 커밋의 파일만 `ehyun` 에 다시 올리고 `develop` 을 되감아 머지했다.

**했어야 할 것**
- 커밋 직전에 `git branch --show-current` 출력을 확인한다.
- 푸시 후에는 `git rev-parse v2/<브랜치>` 로 원격이 실제로 움직였는지 본다.

### 21. 머지가 충돌로 실패했는데 푸시가 실행됐다 (2026-09-23)

**무슨 일** `main` 에서 딴 핫픽스 브랜치를 `develop` 에 머지했다. `models.ts` 에서
충돌이 났는데 뒤에 붙인 `git push v2 develop hotfix-...` 가 실행됐다. `develop` 은
원격과 같아서 바뀐 것은 없었지만 `develop` 워크트리가 충돌 상태로 남았다.

**왜** `set -euo pipefail` 을 켰으니 멈출 거라 믿었다. 그런데 **`a && b` 목록 안의
실패는 `set -e` 가 잡지 않는다.** 목록 전체가 실패해도 다음 줄로 넘어간다.
충돌 자체는, `main` 에서 딴 브랜치를 통째로 머지해 `main` 에만 있는 커밋까지 끌고
온 탓이다.

**복구** `git merge --abort` (`.gitignore` 로컬 변경은 남았다). 수정 커밋 하나만
`develop` 에서 딴 브랜치에 `cherry-pick -x` 해서 머지했다.

**했어야 할 것**
- 되돌리기 어려운 명령(`push`, `reset`, `merge`)은 **한 줄에 하나씩** 쓴다. `&&` 로
  잇지 않는다. 그래야 `set -e` 가 실제로 멈춘다.
- `main` 기준 핫픽스를 `develop` 에 넣을 때는 브랜치가 아니라 **커밋을 옮긴다.**

---

## P2 — "검증했다" 고 했지만 눈으로만 봤다

### 4. 배송이력관리 페이지가 흰 화면으로 배포됐다

**무슨 일** 페이지를 열면 아무것도 안 나왔다. 강력 새로고침도 소용없었다.
사용자가 발견했다.

**왜** `createEmptyDay(date)` 를 `farms` 없이 부른 자리가 있어 빈 `cells` 가
만들어졌고, `farmDayTotal(cells[ch].count)` 에서 터졌다. **브라우저로 한 번도
열어보지 않고 머지했다.**

**고침** 호출부를 고치고 방어적으로 읽게 했다(`93e1f5c`). 이 일 뒤에 Playwright 를
붙였다.

### 5. 보내는 분 주소가 이틀 넘게 저장되지 않았다

**무슨 일** 주문서의 '현재 위치'·'주소 검색' 버튼에 **아무 동작도 붙어 있지 않았다.**
손님이 보내는 분 주소를 넣을 방법 자체가 없었다. 운영 주문 3건에 주소가 비어 있다.

**왜** 그 UI 는 다른 사람이 넣었지만(2026-08-24 `fb53910`), 나는 그 사이 주문서를
여러 번 고쳤다 — 배송 예상일정, 마감 문구, 배송 희망일정. 매번 **화면이 뜨는지와
내가 바꾼 줄이 보이는지만** 봤고 **버튼을 눌러본 적이 없다.** 눌렀으면 즉시 드러났다.

또 하나: `create-order` 는 늘 API 로만 호출해 확인했다. **화면의 입력이 그 API 까지
값을 실어 보내는지는 검증한 적이 없다.** 이번 사고가 정확히 그 틈이다.

**했어야 할 것**
- 화면을 고쳤으면 **그 화면의 버튼을 눌러서** 통과시킨다. 렌더링 확인은 검증이 아니다.
- 주문서를 건드렸으면 **장바구니 → 입력 → 주문 완료까지 실제로 끝낸다.**
- 그리고 **DB 에 무엇이 들어갔는지** 확인한다. 화면에 보이는 것과 저장된 것은 다르다.

### 6. 달력에 공휴일을 넣지 않고 완료라고 했다

**무슨 일** 배송이력관리 달력을 만들어 놓고 공휴일 표시를 넣지 않았다. 사용자가
지적했다 — *"저기에 있는 달력도 공휴일 불러와서 빨간 글자로 표시하고 어떤
공휴일인지도 작은 글자로 표시해 이미 구현되어 있으니까 재활용 하면 됨."*

**왜** 이미 다른 화면에 있던 `useHolidays` 를 찾아보지 않았다. 화면이 뜨는 것으로
끝났다고 봤다.

**했어야 할 것** 같은 성격의 화면이 이미 있으면 그것을 먼저 열어 본다. 완료라고
하기 전에 **기존 화면과 나란히 놓고** 빠진 것을 센다.

### 7. 농가 수정 저장 버튼이 먹지 않는 것을 사용자가 발견했다

**무슨 일** 개발 서버에서 농가 정보를 저장하면 `invalid input syntax for type json`
이 떴다. 저장이 아예 되지 않았다. **사용자가 발견해 알려 줬다** — *"개발서버에서
저장 버튼이 안먹혀."*

**왜** 드라이버가 JS 배열을 PostgreSQL 배열 리터럴(`{a,b}`)로 바꿔 보내는데 jsonb
컬럼은 그것을 받지 못한다. Node 에서 Python 으로 옮기고 나서 **저장 버튼을 한 번도
눌러 보지 않았다.** 눌렀으면 즉시 나왔다.

**고침** json/jsonb 컬럼 목록을 기동 시 캐시에 담아, 바인딩할 때 해당 컬럼이면
JSON 문자열로 만들어 보낸다.

### 8. 실행해 보지 않은 백업 스크립트를 커밋했다

**무슨 일** 백업 스크립트의 로그가 건수 없이 `개` 만 찍혔다.

**왜** `$COPIED개` 라고 써서 셸이 `COPIED개` 를 변수명으로 읽었다. 그런 변수는 없으니
빈 값이 된다. **스크립트를 한 번도 돌려 보지 않고 커밋했다.**

**고침** `${COPIED}개`.

**규칙** 셸 변수 뒤에 한글이 붙으면 반드시 `${}` 로 감싼다. 그리고 스크립트는
커밋 전에 **실행해서 출력을 본다.**

---

**이 다섯 건에 대해 사용자가 한 지적** — *"검증은 하고 올렸냐"*, *"실제 브라우저에서
확인하는건 당연한거야"*, *"왜 자꾸 대충하는거야"*, *"개발서버에서 저장 버튼이
안먹혀"*. 확인한 범위를 넘겨 말하지
않는다. 화면만 봤으면 "화면만 봤다" 고 적는다.

---

## P3 — 되돌릴 수 없는 것을 먼저 백업하지 않았다

### 9. `.env` 를 사본 없이 건드렸다

사고 1번과 같은 건이다. 파일을 건드리기 전에 복사해 뒀으면 몇 시간을 쓰지 않았다.
사용자가 다시 시도할 때 못을 박았다 — *".env 3개 무조건 백업하고 진행해 반드시
무조건."*

### 10. DB 를 백업 없이 고치고 있었다

마이그레이션을 되돌릴 방법 없이 적용하고 있었다. 사용자가 요구해서야 만들었다 —
*"데이터베이스를 건드리는 매 작업마다 데이터베이스 백업해서 되돌릴 수 있게 해줘."*

그 뒤로 `server/db/snapshot.sh` 로 이름 붙인 스냅샷을 뜬다. 지금까지 24개가 쌓였다.
`.env` 3개도 매일 04:30 백업에 포함시켰다.

**규칙** DB 스키마·데이터, `.env`, 키 — 건드리기 전에 스냅샷. 예외 없다.

---

## P4 — 확인하지 않고 단정했다

### 11. 마이그레이션이 `bankda` 를 체크 제약에서 빼먹었다

`deposit_callback_provider` 를 손보면서 허용 목록에서 `'bankda'` 를 떨어뜨렸다.
그대로 운영에 갔으면 **자동 입금 대사가 전부 멈췄다.** 기존 6행이 제약을 위반하는
것을 롤백 테스트에서 잡아 되살렸다(`3e519d8`).

**규칙** 체크 제약을 바꾸면 **기존 행이 그 제약을 통과하는지** 먼저 센다.

### 12. 내 테스트 스크립트가 지운 행을 두고 "저장이 안 된다" 고 했다

"저장은 되는데 새로고침하면 0 이 된다" 고 보고했다. 실제로는 **내 스크립트가 매
실행 전에 그 행을 지우고 있었다.** 코드에는 문제가 없었다.

**규칙** 재현 스크립트가 스스로 상태를 바꾸고 있지 않은지 먼저 본다.

### 13. 편집할 수 없는 칸을 재고 "값이 안 들어간다" 고 했다

입력 대상이 아닌 `—` 표시 칸을 읽고 있었다. 애초에 값이 들어갈 수 없는 자리다.

**규칙** 증상을 말하기 전에 **로그와 DB 로 한 번 더** 확인한다.

### 14. 컬럼을 테이블로 조회해 "운영에 마이그레이션이 빠졌다" 고 할 뻔했다

`to_regclass('public.shipping_fees')` 로 확인했는데 `shipping_fees` 는 **테이블이
아니라 `products` 의 컬럼**이다. 없는 것으로 보여 하마터면 운영에 잘못된 조치를
할 뻔했다.

**왜 이런 확인을 손으로 하고 있었나** 운영 DB 에 마이그레이션 추적 테이블
(`supabase_migrations.schema_migrations`)이 없다. 무엇이 적용됐는지 매번 컬럼
존재로 짐작한다. **아직 고치지 않은 문제다.**

### 15. 비어 있던 `user.name` 을 맥 계정명으로 채워 커밋했다

git 설정이 비어 있자 맥 계정명 `lkim` 을 임의로 넣어 커밋했다. **커밋 14개를 다시
써야 했다.** 물어봤으면 끝날 일이었다.

**규칙** 설정이 비어 있으면 채우지 말고 묻는다.

### 16. 이미 인코딩된 API 키를 다시 인코딩해 403 을 받았다

**무슨 일** 공휴일 API 가 계속 403 을 돌려줬다.

**왜** 공공데이터포털이 주는 키는 **이미 URL 인코딩돼 있다.** 그것을 모른 채
`httpx` 에 넘겼고, `%` 가 한 번 더 인코딩됐다. 키 형태를 보지 않고 문자열로만 다뤘다.

**고침** `unquote()` 로 풀어서 넣는다.

**규칙** 외부 키가 403 을 내면 **키의 실제 바이트를 먼저 본다.** 인코딩된 채로
발급되는 곳이 있다.

### 17. 드라이버가 같은 타입을 같게 다룰 것이라 여겼다

**무슨 일** Node 에서 Python 으로 옮기는 중 `date` 컬럼이 어긋났다. asyncpg 는
`date` 객체를 원하고 이전 드라이버는 JS `Date` 를 주고 있었다.

**왜** 두 드라이버가 같은 타입을 같게 직렬화할 것이라 여겼다. 확인하지 않았다.

**고침** `'YYYY-MM-DD'` 문자열로 통일했다. uuid·int8·numeric·timestamptz 도 같은
이유로 코덱을 맞췄고, 102개 사례를 양쪽에서 돌려 바이트가 같은 것을 확인한 뒤
넘어갔다.

**규칙** 드라이버를 바꾸면 타입마다 **양쪽 출력을 실제로 비교한다.**

### 18. 무엇이 커밋되는지 보지 않고 `__pycache__` 19개를 올렸다

**무슨 일** 파이썬 캐시 파일 19개가 저장소에 들어갔다.

**왜** `.gitignore` 패턴이 한 경로만 잡고 있었는데, **`git status` 를 보지 않고
`git add` 했다.**

**규칙** 커밋 전에 `git status --short` 로 **들어가는 파일 목록을 눈으로 본다.**

### 22. 부하 시험 수치를 재 보지도 않은 병목 탓으로 돌렸다 (2026-09-23)

**무슨 일** 직접 짠 파이썬(httpx) 부하 스크립트로 "동시 100명 p95 1.5초, 병목은
DB 풀" 이라고 보고했고, 사용자가 그 말을 믿고 풀을 30 으로 올려 달라고 했다.
ab 로 다시 재니 같은 서버가 p95 49ms 였다. 느렸던 것은 **부하를 만드는 스크립트**
였고, 풀을 늘려도 달라지지 않았다. 올린 커밋은 푸시 전에 되돌렸다.

**왜** 측정 도구가 측정 대상보다 먼저 한계에 닿을 수 있다는 것을 확인하지 않았다.
생성기와 서버가 같은 2코어 VM 에서 CPU 를 나눠 썼는데도 수치를 서버 탓으로 읽었다.

**했어야 할 것**
- 부하는 검증된 도구(ab, wrk, oha)로 건다.
- 결론을 말하기 전에 **서버 프로세스의 CPU** 를 같이 잰다. 서버가 한가한데 느리면
  측정 쪽을 먼저 의심한다.

## P5 — 확인 없이 덮어썼다

### 19. 방금 만든 문서를 `git checkout` 으로 날렸다

문서에 코드 링크를 붙인 뒤 `git checkout develop -- doc` 을 실행했다. 커밋되지
않은 그 수정이 **develop 의 옛 버전으로 덮여 사라졌다.** 스크립트로 만든 것이라
다시 돌려 복구했지만, 손으로 쓴 것이었으면 그대로 잃었다.

**규칙** `git checkout <ref> -- <경로>` 는 **작업 내용을 지우는 명령**이다.
`git status` 로 그 경로에 커밋 안 된 변경이 있는지 먼저 본다.

### 20. `git branch -f` 로 두 브랜치를 강제로 붙였다

`git branch -f ehyun ui` 를 실행해 백엔드 브랜치와 화면 브랜치의 구분을 없앴다.
브랜치를 나눠 둔 이유 자체를 지운 것이다. 사용자가 직접 지적했다.

**규칙** `branch -f` 는 이력을 덮는 명령이다. 쓰지 않는다.

---

## 작업 전 점검표

작업을 시작하기 전에 이 목록을 지나간다.

**시작할 때**
- [ ] 되돌릴 수 없는 것(`.env`, DB, 키)을 건드리나? → 먼저 백업/스냅샷
- [ ] 지금 브랜치가 맞나? `git branch --show-current` 출력을 눈으로 확인
- [ ] 운영에 영향이 가나? → 승인부터 받는다

**명령을 칠 때**
- [ ] `set -euo pipefail`, 명령은 `&&` 로 연결
- [ ] `reset --hard`, `checkout -- <경로>`, `ln -sfn`, `delete` — 대상 먼저 출력
- [ ] 실패 메시지가 보이면 **거기서 멈춘다.** 뒤 명령이 성공한 것처럼 보여도 믿지 않는다

**검증할 때**
- [ ] 화면을 고쳤으면 **눌러서** 확인했나? 렌더링만 본 것은 검증이 아니다
- [ ] 주문서를 건드렸으면 **주문을 끝까지** 넣어 봤나?
- [ ] **DB 에 무엇이 들어갔는지** 확인했나?
- [ ] 검증에 쓴 데이터를 되돌렸나?

**보고할 때**
- [ ] 확인한 범위만 말한다. 못 한 것은 못 했다고 적는다
- [ ] 단정하기 전에 쿼리/로그로 한 번 더 확인한다

---

## 아직 남은 위험

- 운영 DB 에 **마이그레이션 추적 테이블이 없다.** 적용 여부를 매번 짐작한다.
- 백업 사본이 **이 맥 한 대에만** 있다. 디스크가 죽으면 원본과 백업이 함께 사라진다.
- 노출된 비밀(Gmail 앱 비밀번호, Naver Client Secret)을 아직 교체하지 않았다.
